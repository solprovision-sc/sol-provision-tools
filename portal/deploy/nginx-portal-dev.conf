# Provisioner Portal — DEV
#
# Install to /etc/nginx/sites-available/solprovision-portal-dev and symlink into
# sites-enabled. This is the PRE-CERTBOT form: plain :80 only. Run
#   sudo certbot --nginx -d portal-dev.solprovision.com
# afterwards and it will add the listen 443 / ssl_certificate lines and the
# http->https redirect, exactly as it did for the tools server blocks.
#
# Ports on this box: 5000 tools prod, 5001 tools dev, 5002 portal dev
# (5003 reserved for portal prod).

server {
    listen 80;
    server_name portal-dev.solprovision.com;

    # Portal's own static assets, served off disk rather than through Flask —
    # same pattern as the tools blocks.
    location /static/ {
        alias /var/www/sol-provision-tools-dev/portal/static/;
        access_log off;
        expires 30d;
    }

    # Shared brand layer (brand.css + woff2 faces). The app also serves this
    # itself at /brand/, so this block is purely an optimisation — but it must
    # point at the SHARED directory, not the portal's static folder.
    location /brand/ {
        alias /var/www/sol-provision-tools-dev/shared/brand/;
        access_log off;
        expires 30d;
    }

    location / {
        proxy_pass         http://127.0.0.1:5002;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
}
