# ── Database (never commit — SCP'd to server separately) ─────────────────────
*.db
*.db-shm
*.db-wal

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# ── Virtual environments ──────────────────────────────────────────────────────
venv/
.venv/
env/
.env/

# ── Local config / secrets (server IPs, API keys — never commit) ─────────────
.env
*.env
config.local.py
secrets.py

# ── DataForge / pipeline artifacts ───────────────────────────────────────────
*.p4k
*.dcb
*.log
exports/
DataMining/

# ── OS noise ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor artifacts ──────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── Future build tooling (Vite/Node — cargo planner rebuild) ─────────────────
node_modules/
dist/
.cache/